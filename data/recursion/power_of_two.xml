<content>
  <name>Power of Two</name>
  <description>
    Checks if a number is a power of two using recursion.
  </description>
  <script>
  <![CDATA[
    /**
     * Checks if a number is a power of two using recursion.
     * @param {number} n - The number to check.
     * @returns {boolean} - True if n is a power of two, false otherwise.
     */
    function isPowerOfTwo(n) {
        if (n < 1) return false;
        if (n === 1) return true;
        return isPowerOfTwo(n / 2);
    }

    console.log(isPowerOfTwo(16)); // Outputs: true
    console.log(isPowerOfTwo(18)); // Outputs: false
  ]]>
  </script>
</content>