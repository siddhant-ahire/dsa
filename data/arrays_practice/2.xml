<content>
  <name>Min and Max Number</name>
  <description>

  </description>
  <script>
  <![CDATA[
    function findMaxMin(arr) {
        let max = arr[0]; // Initialize max with the first element
        let min = arr[0]; // Initialize min with the first element

        for (let i = 1; i < arr.length; i++) {
            if (arr[i] > max) {
                max = arr[i]; // Update max if current element is larger
            }
            if (arr[i] < min) {
                min = arr[i]; // Update min if current element is smaller
            }
        }

        return { max, min };
    }
    const array = [10, 2, 5, 23, 7, 1, 19];
    const { max, min } = findMaxMin(array);
    console.log(`Maximum: ${max}, Minimum: ${min}`);
    ]]>
  </script>
</content>